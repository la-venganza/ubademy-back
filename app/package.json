{
  "name": "ubademy-back",
  "version": "1.0.0",
  "description": "Ubademy - Coursera like application backend.",
  "main": "app.js",
  "scripts": {
    "start": "cross-env NODE_ENV=standalone nodemon server.js",
    "start:dev": "cross-env NODE_ENV=standalone nodemon server.js",
    "start:prod": "cross-env NODE_ENV=dockerized nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "config": "^3.3.6",
    "cookie-session": "^1.4.0",
    "express": "^4.17.1",
    "nodemon": "^2.0.13",
    "passport": "^0.5.0",
    "passport-google-oauth20": "^2.0.0",
    "properties-reader": "^2.2.0",
    "request": "2.88.2",
    "sleep": "^6.3.0",
    "swagger-ui-express": "^4.1.6"
  },
  "devDependencies": {
    "cross-env": "^7.0.2"
  }
}
